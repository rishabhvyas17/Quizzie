<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Classes - Quizzie</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #F1F6F9;
            --primary: #14274E;
            --secondary: #394867;
            --muted: #9BA4B4;
            --white: #ffffff;
            --card-shadow: 0 2px 16px rgba(20, 39, 78, 0.07);
            --card-radius: 18px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg);
            color: var(--primary);
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }

        /* === HEADER === */
        .header {
            background: var(--white);
            box-shadow: 0 2px 20px rgba(20, 39, 78, 0.06);
            padding: 0.75rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.6rem;
            font-weight: 800;
            color: var(--primary);
            letter-spacing: -0.5px;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .logout-btn {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            padding: 0.5rem 1.25rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 0.9rem;
            text-decoration: none;
            transition: all 0.2s ease;
            display: inline-flex;
            align-items: center;
            border: none;
        }

        .logout-btn:hover {
            background: #ef4444;
            color: white;
        }

        .header-profile {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #7c5cfc, #a78bfa);
            border-radius: 50%;
            background: linear-gradient(135deg, #394867 0%, #14274E 100%);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            text-decoration: none;
            margin-left: 0.75rem;
            border: 2px solid #F1F6F9;
            box-shadow: 0 2px 8px rgba(20, 39, 78, 0.15);
            transition: all 0.2s;
        }

        .header-profile:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 12px rgba(20, 39, 78, 0.25);
        }

        .header-icon {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 12px;
            background: #f0f2f7;
            color: var(--secondary);
            text-decoration: none;
            transition: all 0.2s;
        }

        .header-icon:hover {
            background: #e2e8f0;
            color: var(--primary);
        }

        .nav-links {
            display: flex;
            gap: 0.25rem;
            list-style: none;
            margin: 0;
            padding: 4px;
            background: #f0f2f7;
            border-radius: 12px;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--muted);
            font-weight: 500;
            font-size: 0.9rem;
            padding: 0.5rem 1.25rem;
            border-radius: 10px;
            transition: all 0.25s ease;
            cursor: pointer;
        }

        .nav-links a:hover {
            color: var(--primary);
            background: var(--bg);
        }

        .nav-links a.active {
            color: white;
            background: #1e1e2e;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
        }

        /* === HAMBURGER MENU === */
        .hamburger-btn {
            display: none;
            width: 38px;
            height: 38px;
            border-radius: 10px;
            background: var(--bg);
            border: none;
            color: var(--secondary);
            cursor: pointer;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

        .hamburger-btn:hover {
            background: #e2e8f0;
            color: var(--primary);
        }

        .mobile-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(20, 39, 78, 0.4);
            backdrop-filter: blur(2px);
            z-index: 998;
        }

        .mobile-overlay.open {
            display: block;
        }

        .mobile-nav {
            position: fixed;
            top: 0;
            right: -65%;
            width: 65%;
            max-width: 300px;
            height: 100%;
            background: var(--white);
            z-index: 999;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
            box-shadow: -4px 0 24px rgba(20, 39, 78, 0.15);
            transition: right 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            overflow-y: auto;
        }

        .mobile-nav.open {
            right: 0;
        }

        .mobile-nav-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.25rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid rgba(155, 164, 180, 0.15);
        }

        .mobile-nav-header .logo {
            font-size: 1.3rem;
        }

        .mobile-nav-close {
            width: 34px;
            height: 34px;
            border-radius: 10px;
            background: var(--bg);
            border: none;
            color: var(--secondary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

        .mobile-nav-close:hover {
            background: #fee2e2;
            color: #ef4444;
        }

        .mobile-nav a {
            text-decoration: none;
            color: var(--secondary);
            font-weight: 500;
            font-size: 0.95rem;
            padding: 0.75rem 1rem;
            border-radius: 12px;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            gap: 0.6rem;
        }

        .mobile-nav a:hover,
        .mobile-nav a.active {
            color: var(--primary);
            background: var(--bg);
            font-weight: 600;
        }

        .mobile-nav-divider {
            height: 1px;
            background: rgba(155, 164, 180, 0.15);
            margin: 0.5rem 0;
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: row;
                flex-wrap: nowrap;
            }

            .header-content nav {
                display: none;
            }

            .header-right .header-profile {
                display: none;
            }

            .hamburger-btn {
                display: flex;
            }
        }

        /* === MAIN === */
        .main-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 2rem 1.5rem;
            flex: 1;
            width: 100%;
        }

        .dashboard-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 1.75rem;
            letter-spacing: -0.3px;
        }

        /* === BENTO GRID === */
        .bento-grid {
            display: grid;
            grid-template-columns: repeat(12, 1fr);
            gap: 1.25rem;
            margin-bottom: 2rem;
        }

        .bento-card {
            background: var(--white);
            border-radius: var(--card-radius);
            padding: 1.5rem;
            box-shadow: var(--card-shadow);
            border: 1px solid rgba(155, 164, 180, 0.15);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .bento-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(20, 39, 78, 0.1);
        }

        .stat-card {
            grid-column: span 3;
            text-align: center;
        }

        .stat-icon {
            width: 48px;
            height: 48px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 1.5rem;
        }

        .bg-blue {
            background: rgba(20, 39, 78, 0.1);
            color: var(--primary);
        }

        .bg-green {
            background: rgba(16, 185, 129, 0.1);
            color: #10b981;
        }

        .bg-purple {
            background: rgba(139, 92, 246, 0.1);
            color: #8b5cf6;
        }

        .bg-orange {
            background: rgba(245, 158, 11, 0.1);
            color: #f59e0b;
        }

        .stat-value {
            font-size: 2.25rem;
            font-weight: 800;
            color: var(--primary);
            margin-bottom: 0.25rem;
        }

        .stat-label {
            font-size: 0.85rem;
            color: var(--muted);
            font-weight: 600;
        }

        /* === CLASSES SECTION === */
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            margin-top: 1rem;
        }

        .section-title {
            font-size: 1.35rem;
            font-weight: 700;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .create-class-btn {
            background: linear-gradient(135deg, #10b981, #34d399);
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
        }

        .create-class-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(16, 185, 129, 0.3);
            background: linear-gradient(135deg, #059669, #10b981);
        }

        .classes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.25rem;
        }

        .class-card {
            background: var(--white);
            border-radius: var(--card-radius);
            padding: 1.5rem;
            box-shadow: var(--card-shadow);
            border: 1px solid rgba(155, 164, 180, 0.15);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .class-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: var(--primary);
        }

        .class-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(20, 39, 78, 0.1);
        }

        .class-header {
            margin-bottom: 1.25rem;
        }

        .class-name {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.25rem;
        }

        .class-subject {
            font-size: 0.85rem;
            color: var(--secondary);
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.35rem;
        }

        .class-description {
            font-size: 0.8rem;
            color: var(--muted);
            line-height: 1.5;
            margin-top: 0.75rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .class-stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0.5rem;
            background: var(--bg);
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1.25rem;
        }

        .class-mini-stat {
            text-align: center;
        }

        .mini-stat-value {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--primary);
            display: block;
        }

        .mini-stat-label {
            font-size: 0.7rem;
            color: var(--muted);
            font-weight: 500;
        }

        .class-actions {
            display: flex;
            gap: 0.75rem;
        }

        .btn {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.7rem 1rem;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.25s;
            font-size: 0.85rem;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            flex: 1;
        }

        .btn:hover {
            background: var(--secondary);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(20, 39, 78, 0.2);
        }

        .btn-outline {
            background: transparent;
            color: var(--primary);
            border: 1px solid rgba(155, 164, 180, 0.4);
        }

        .btn-outline:hover {
            background: var(--bg);
            border-color: var(--primary);
            color: var(--primary);
            box-shadow: none;
        }

        .btn-accent {
            background: linear-gradient(135deg, #f59e0b, #fbbf24);
        }

        .btn-accent:hover {
            background: linear-gradient(135deg, #d97706, #f59e0b);
        }

        /* === MODAL === */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(20, 39, 78, 0.4);
            backdrop-filter: blur(8px);
        }

        .modal.show {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background: white;
            border-radius: 24px;
            padding: 2rem;
            width: 90%;
            max-width: 500px;
            box-shadow: 0 24px 64px rgba(20, 39, 78, 0.2);
            animation: modalSlideIn 0.35s cubic-bezier(.4, 0, .2, 1);
        }

        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(20px) scale(0.97);
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .modal-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--primary);
        }

        .close-btn {
            background: var(--bg);
            border: none;
            font-size: 1.25rem;
            cursor: pointer;
            color: var(--muted);
            width: 36px;
            height: 36px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }

        .close-btn:hover {
            background: #e2e8f0;
            color: var(--primary);
        }

        /* === FORMS === */
        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--secondary);
            font-size: 0.9rem;
        }

        .form-input,
        .form-textarea {
            width: 100%;
            padding: 0.85rem 1rem;
            border: 2px solid rgba(155, 164, 180, 0.3);
            border-radius: 12px;
            font-size: 0.95rem;
            font-family: inherit;
            transition: all 0.2s;
            background: var(--bg);
            color: var(--primary);
        }

        .form-input:focus,
        .form-textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(20, 39, 78, 0.1);
            background: white;
        }

        .form-textarea {
            min-height: 100px;
            resize: vertical;
        }

        /* === NOTIFICATION TOAST === */
        .notification-toast {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 1.25rem 1.5rem;
            border-radius: 16px;
            box-shadow: 0 12px 40px rgba(20, 39, 78, 0.2);
            z-index: 1000;
            max-width: 380px;
            display: flex;
            align-items: center;
            gap: 1rem;
            animation: slideInRight 0.4s cubic-bezier(.4, 0, .2, 1);
            background: white;
            border-left: 4px solid var(--primary);
        }

        .toast-success {
            border-left-color: #10b981;
        }

        .toast-error {
            border-left-color: #ef4444;
        }

        .toast-icon {
            font-size: 1.5rem;
        }

        .toast-success .toast-icon {
            color: #10b981;
        }

        .toast-error .toast-icon {
            color: #ef4444;
        }

        .toast-content {
            flex: 1;
        }

        .toast-title {
            font-weight: 700;
            font-size: 0.95rem;
            color: var(--primary);
            margin-bottom: 0.2rem;
        }

        .toast-message {
            font-size: 0.85rem;
            color: var(--muted);
            line-height: 1.4;
        }

        .toast-close {
            background: none;
            border: none;
            color: var(--muted);
            cursor: pointer;
            font-size: 1.25rem;
            padding: 0.25rem;
        }

        .toast-close:hover {
            color: var(--primary);
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(100%)
            }

            to {
                opacity: 1;
                transform: translateX(0)
            }
        }

        /* === EMPTY STATE === */
        .empty-state {
            grid-column: 1 / -1;
            text-align: center;
            padding: 4rem 2rem;
            background: var(--white);
            border-radius: var(--card-radius);
            box-shadow: var(--card-shadow);
            border: 1px dashed rgba(155, 164, 180, 0.4);
        }

        .empty-icon {
            font-size: 3rem;
            color: var(--muted);
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .empty-state h3 {
            font-size: 1.25rem;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }

        .empty-state p {
            color: var(--muted);
            margin-bottom: 1.5rem;
            font-size: 0.95rem;
        }

        /* === FOOTER === */
        .footer {
            background: var(--white);
            border-top: 1px solid rgba(155, 164, 180, 0.2);
            padding: 1.5rem 0;
            margin-top: auto;
        }

        .footer-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
            color: var(--muted);
        }

        .footer-link {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s;
        }

        .footer-link:hover {
            color: var(--secondary);
        }

        /* === RESPONSIVE === */
        @media(max-width:1024px) {
            .stat-card {
                grid-column: span 6;
            }
        }

        @media(max-width:768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
            }

            .dashboard-title {
                font-size: 1.5rem;
                text-align: center;
            }

            .section-header {
                flex-direction: column;
                gap: 1rem;
                align-items: stretch;
                text-align: center;
            }

            .create-class-btn {
                justify-content: center;
            }

            .classes-grid {
                grid-template-columns: 1fr;
            }

            .modal-content {
                width: 95%;
                padding: 1.5rem;
                border-radius: 20px;
            }

            .footer-content {
                flex-direction: column;
                gap: 0.5rem;
                text-align: center;
            }
        }

        @media(max-width:480px) {
            .stat-card {
                grid-column: span 12;
            }

            .class-actions {
                flex-direction: column;
            }
        }
    </style>
</head>

<body>
    <header class="header">
        <div class="header-content">
            <div class="logo">Quizzie</div>
            <nav>
                <ul class="nav-links">
                    <li><a href="/homeTeacher">Dashboard</a></li>
                    <li><a class="active">My Classes</a></li>
                    <li><a href="/profileTeacher">My Profile</a></li>
                </ul>
            </nav>
            <div class="header-right">
                <a href="/notifications" class="header-icon" title="Notifications"><i data-lucide="bell"
                        style="width:18px;height:18px"></i></a>
                <button class="hamburger-btn" onclick="toggleMobileNav()" title="Menu" id="hamburgerBtn">
                    <i data-lucide="menu" style="width:20px;height:20px"></i>
                </button>
                <div class="profile-section">
                    <a href="/profileTeacher" class="header-profile" title="My Profile" id="profileAvatar">
                        <span class="profile-initials">{{initials}}</span>
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Mobile Overlay -->
    <div class="mobile-overlay" id="mobileOverlay" onclick="closeMobileNav()"></div>

    <!-- Mobile Side Drawer -->
    <div class="mobile-nav" id="mobileNav">
        <div class="mobile-nav-header">
            <div class="logo">Quizzie</div>
            <button class="mobile-nav-close" onclick="closeMobileNav()">
                <i data-lucide="x" style="width:18px;height:18px"></i>
            </button>
        </div>
        <a href="/homeTeacher"><i data-lucide="layout-dashboard" style="width:18px;height:18px"></i>
            Dashboard</a>
        <a href="/teacher/classes" class="active"><i data-lucide="library" style="width:18px;height:18px"></i> My
            Classes</a>
        <a href="/profileTeacher"><i data-lucide="user" style="width:18px;height:18px"></i> My Profile</a>
    </div>

    <main class="main-content">
        <!-- My Classes Section -->
        <section id="my-classes-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i data-lucide="library" style="color: var(--primary);"></i>
                    My Classes
                </h2>
                <button class="create-class-btn" onclick="showCreateClassModal()">
                    <i data-lucide="plus" style="width: 18px; height: 18px;"></i>
                    New Class
                </button>
            </div>

            <div id="classesContainer">
                {{#if classes}}
                <div class="classes-grid">
                    {{#each classes}}
                    <div class="class-card">
                        <div class="class-header">
                            <h3 class="class-name">{{this.name}}</h3>
                            <div class="class-subject">
                                <i data-lucide="book" style="width: 14px; height: 14px;"></i>
                                {{this.subject}}
                            </div>
                            {{#if this.description}}
                            <div class="class-description">{{this.description}}</div>
                            {{/if}}
                        </div>

                        <div class="class-stats-grid">
                            <div class="class-mini-stat">
                                <span class="mini-stat-value">{{this.studentCount}}</span>
                                <span class="mini-stat-label">Students</span>
                            </div>
                            <div class="class-mini-stat">
                                <span class="mini-stat-value">{{this.lectureCount}}</span>
                                <span class="mini-stat-label">Lectures</span>
                            </div>
                            <div class="class-mini-stat">
                                <span class="mini-stat-value">{{toFixed this.averageScore 1}}%</span>
                                <span class="mini-stat-label">Avg Grade</span>
                            </div>
                        </div>

                        <div class="class-actions">
                            <a href="/class/manage/{{this.id}}" class="btn btn-outline">
                                <i data-lucide="settings" style="width: 16px; height: 16px;"></i>
                                Manage
                            </a>
                            <button class="btn btn-accent" onclick="redirectToCreateQuiz('{{this.id}}')">
                                <i data-lucide="plus-circle" style="width: 16px; height: 16px;"></i>
                                Quiz
                            </button>
                        </div>
                    </div>
                    {{/each}}
                </div>
                {{else}}
                <div class="empty-state">
                    <i data-lucide="school" class="empty-icon"></i>
                    <h3>No Classes Yet</h3>
                    <p>Create your first class to start organizing your lectures and students!</p>
                    <button class="create-class-btn" style="margin: 0 auto;" onclick="showCreateClassModal()">
                        <i data-lucide="plus" style="width: 18px; height: 18px;"></i>
                        Create Your First Class
                    </button>
                </div>
                {{/if}}
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="footer-content">
            <div class="footer-copyright">
                © 2025 All rights reserved by www.Quizzie.in
            </div>
            <div class="footer-links">
                <a href="/about-developers" class="footer-link">Meet Our Developers</a>
            </div>
        </div>
    </footer>

    <!-- Create Class Modal -->
    <div id="createClassModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">Create New Class</h2>
                <button class="close-btn" onclick="hideCreateClassModal()">
                    <i data-lucide="x"></i>
                </button>
            </div>
            <form id="createClassForm">
                <div class="form-group">
                    <label class="form-label">Class Name <span style="color: #ef4444;">*</span></label>
                    <input type="text" id="className" class="form-input" placeholder="e.g., Advanced Physics 301"
                        required>
                </div>
                <div class="form-group">
                    <label class="form-label">Subject <span style="color: #ef4444;">*</span></label>
                    <input type="text" id="classSubject" class="form-input" placeholder="e.g., Physics" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Description</label>
                    <textarea id="classDescription" class="form-textarea"
                        placeholder="Brief description of the class (optional)"></textarea>
                </div>
                <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
                    <button type="button" class="btn btn-outline" onclick="hideCreateClassModal()">Cancel</button>
                    <button type="submit" class="btn"
                        style="background: linear-gradient(135deg, #10b981, #34d399); border: none;">
                        <i data-lucide="check" style="width: 16px; height: 16px;"></i> Create Class
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Global variables
        const professorName = '{{{userName}}}';

        document.addEventListener('DOMContentLoaded', function () {
            loadClasses();
            setProfileInitials();
        });

        // Mobile sidebar toggle
        function toggleMobileNav() {
            document.getElementById('mobileNav').classList.add('open');
            document.getElementById('mobileOverlay').classList.add('open');
            document.body.style.overflow = 'hidden';
            lucide.createIcons();
        }

        function closeMobileNav() {
            document.getElementById('mobileNav').classList.remove('open');
            document.getElementById('mobileOverlay').classList.remove('open');
            document.body.style.overflow = '';
        }

        function setProfileInitials() {
            const name = professorName || 'Teacher';
            const initials = name.split(' ').map(n => n[0]).join('').substring(0, 2).toUpperCase();
            document.querySelectorAll('.profile-initials').forEach(el => el.textContent = initials);
        }


        async function loadClasses() {
            try {
                const response = await fetch('/api/classes');
                const result = await response.json();

                if (result.success) {
                    displayClasses(result.classes);
                } else {
                    showToast('Error', 'Failed to load classes: ' + result.message, 'error');
                }
            } catch (error) {
                console.error('Error loading classes:', error);
                showToast('Error', 'Network error while loading classes.', 'error');
            }
        }

        function displayClasses(classes) {
            const container = document.getElementById('classesContainer');

            if (classes.length === 0) {
                container.innerHTML = `
                    <div class="empty-state">
                        <i data-lucide="school" class="empty-icon"></i>
                        <h3>No Classes Yet</h3>
                        <p>Create your first class to start organizing your lectures and students!</p>
                        <button class="create-class-btn" style="margin: 0 auto;" onclick="showCreateClassModal()">
                            <i data-lucide="plus" style="width: 18px; height: 18px;"></i>
                            Create Your First Class
                        </button>
                    </div>
                `;
                lucide.createIcons();
                return;
            }

            const classesHTML = classes.map(classItem => `
                <div class="class-card">
                    <div class="class-header">
                        <h3 class="class-name">${classItem.name}</h3>
                        <div class="class-subject">
                            <i data-lucide="book" style="width: 14px; height: 14px;"></i>
                            ${classItem.subject}
                        </div>
                        ${classItem.description ? `<div class="class-description">${classItem.description}</div>` : ''}
                    </div>
                    
                    <div class="class-stats-grid">
                        <div class="class-mini-stat">
                            <span class="mini-stat-value">${classItem.studentCount}</span>
                            <span class="mini-stat-label">Students</span>
                        </div>
                        <div class="class-mini-stat">
                            <span class="mini-stat-value">${classItem.lectureCount}</span>
                            <span class="mini-stat-label">Lectures</span>
                        </div>
                        <div class="class-mini-stat">
                            <span class="mini-stat-value">${(parseFloat(classItem.averageScore) || 0).toFixed(1)}%</span>
                            <span class="mini-stat-label">Avg Grade</span>
                        </div>
                    </div>
                    
                    <div class="class-actions">
                        <a href="/class/manage/${classItem.id}" class="btn btn-outline">
                            <i data-lucide="settings" style="width: 16px; height: 16px;"></i>
                            Manage
                        </a>
                        <button class="btn btn-accent" onclick="redirectToCreateQuiz('${classItem.id}')">
                            <i data-lucide="plus-circle" style="width: 16px; height: 16px;"></i>
                            Quiz
                        </button>
                    </div>
                </div>
            `).join('');

            container.innerHTML = `<div class="classes-grid">${classesHTML}</div>`;
            lucide.createIcons();
        }

        function updateDashboardStats(classes) {
            const totalClasses = classes.length;
            const totalStudents = classes.reduce((sum, cls) => sum + cls.studentCount, 0);
            const totalLectures = classes.reduce((sum, cls) => sum + cls.lectureCount, 0);
            const totalQuizzes = classes.reduce((sum, cls) => sum + cls.quizCount, 0);

            document.getElementById('totalClassesCount').textContent = totalClasses;
            document.getElementById('totalStudentsCount').textContent = totalStudents;
            document.getElementById('totalLecturesCount').textContent = totalLectures;
            document.getElementById('totalQuizzesCount').textContent = totalQuizzes;
        }

        function showCreateClassModal() {
            document.getElementById('createClassModal').classList.add('show');
            document.getElementById('className').focus();
        }

        function hideCreateClassModal() {
            document.getElementById('createClassModal').classList.remove('show');
            document.getElementById('createClassForm').reset();
        }

        document.getElementById('createClassForm').addEventListener('submit', async function (e) {
            e.preventDefault();

            const formData = {
                name: document.getElementById('className').value.trim(),
                subject: document.getElementById('classSubject').value.trim(),
                description: document.getElementById('classDescription').value.trim()
            };

            if (!formData.name || !formData.subject) {
                showToast('Error', 'Class name and subject are required.', 'error');
                return;
            }

            const submitButton = this.querySelector('button[type="submit"]');
            const originalContent = submitButton.innerHTML;

            try {
                submitButton.innerHTML = '<i data-lucide="loader-2" class="lucide-spin" style="width: 16px; height: 16px; margin-right: 6px;"></i> Creating...';
                lucide.createIcons();
                submitButton.disabled = true;

                const response = await fetch('/api/classes', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();

                if (result.success) {
                    showToast('Success', `Class "${result.class.name}" created!`, 'success');
                    hideCreateClassModal();
                    loadClasses();
                } else {
                    showToast('Error', 'Failed to create class: ' + result.message, 'error');
                }
            } catch (error) {
                showToast('Error', 'Network error while creating class.', 'error');
            } finally {
                submitButton.innerHTML = originalContent;
                submitButton.disabled = false;
                lucide.createIcons();
            }
        });

        function redirectToCreateQuiz(classId) {
            window.location.href = `/class/manage/${classId}?tab=quizzes`;
        }

        function showToast(title, message, type = 'success') {
            document.querySelectorAll('.notification-toast').forEach(t => t.remove());

            const icon = type === 'success' ? 'check-circle' : 'alert-circle';
            const toastClass = type === 'success' ? 'toast-success' : 'toast-error';

            const toastHTML = `
                <div class="notification-toast ${toastClass}">
                    <i data-lucide="${icon}" class="toast-icon"></i>
                    <div class="toast-content">
                        <div class="toast-title">${title}</div>
                        <div class="toast-message">${message}</div>
                    </div>
                    <button class="toast-close" onclick="this.parentElement.remove()">
                        <i data-lucide="x" style="width: 18px; height: 18px;"></i>
                    </button>
                </div>
            `;

            document.body.insertAdjacentHTML('beforeend', toastHTML);
            lucide.createIcons();

            setTimeout(() => {
                const toast = document.querySelector('.notification-toast');
                if (toast) {
                    toast.style.opacity = '0';
                    toast.style.transform = 'translateX(100%)';
                    toast.style.transition = 'all 0.3s ease';
                    setTimeout(() => toast.remove(), 300);
                }
            }, 5000);
        }

        window.onclick = function (event) {
            const modal = document.getElementById('createClassModal');
            if (event.target === modal) {
                hideCreateClassModal();
            }
        }

        document.addEventListener('keydown', function (event) {
            if (event.key === 'Escape') {
                hideCreateClassModal();
            }
        });

        // Add CSS for spinner
        const style = document.createElement('style');
        style.textContent = `
            .lucide-spin { animation: spin 2s linear infinite; }
            @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
        `;
        document.head.appendChild(style);

        // Initialize Lucide icons
        lucide.createIcons();
    </script>
</body>

</html>