<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coming Soon - Quizzie</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #F1F6F9;
            --primary: #14274E;
            --secondary: #394867;
            --muted: #9BA4B4;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg);
            color: var(--primary);
            min-height: 100vh;
        }

        /* === HEADER === */
        .header {
            background: var(--white);
            border-bottom: 1px solid rgba(155, 164, 180, 0.2);
            padding: 0.75rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 12px rgba(20, 39, 78, 0.04);
        }

        .header-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--primary);
            letter-spacing: -0.5px;
        }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 0.5rem;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--muted);
            font-weight: 500;
            font-size: 0.85rem;
            padding: 0.5rem 1rem;
            border-radius: 10px;
            transition: all 0.2s;
            cursor: pointer;
        }

        .nav-links a:hover {
            color: var(--primary);
            background: var(--bg);
        }

        .nav-links a.active {
            color: var(--primary);
            background: var(--bg);
            font-weight: 600;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .header-icon {
            width: 38px;
            height: 38px;
            border-radius: 12px;
            border: none;
            background: var(--bg);
            color: var(--secondary);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s;
        }

        .header-icon:hover {
            background: #e2e8f0;
            color: var(--primary);
        }

        .header-profile {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #394867 0%, #14274E 100%);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            text-decoration: none;
            margin-left: 0.5rem;
            border: 2px solid #F1F6F9;
            box-shadow: 0 2px 8px rgba(20, 39, 78, 0.15);
            transition: all 0.2s;
        }

        .header-profile:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 12px rgba(20, 39, 78, 0.25);
        }

        /* === COMING SOON === */
        .coming-soon-container {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: calc(100vh - 140px);
            padding: 2rem;
        }

        .coming-soon-card {
            background: var(--white);
            border-radius: 28px;
            padding: 3.5rem;
            text-align: center;
            max-width: 520px;
            width: 100%;
            box-shadow: 0 4px 24px rgba(20, 39, 78, 0.06);
            border: 1px solid rgba(155, 164, 180, 0.15);
        }

        .icon-container {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--bg) 0%, rgba(20, 39, 78, 0.08) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            animation: float 3s ease-in-out infinite;
        }

        .icon-container i,
        .icon-container svg {
            color: var(--primary);
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        .coming-soon-title {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary);
            margin-bottom: 0.75rem;
            letter-spacing: -0.5px;
        }

        .coming-soon-subtitle {
            font-size: 1rem;
            color: var(--secondary);
            margin-bottom: 0.5rem;
            font-weight: 500;
        }

        .coming-soon-desc {
            font-size: 0.9rem;
            color: var(--muted);
            line-height: 1.7;
            margin-bottom: 2rem;
        }

        .feature-chips {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            justify-content: center;
            margin-bottom: 2rem;
        }

        .feature-chip {
            display: flex;
            align-items: center;
            gap: 0.4rem;
            padding: 0.45rem 0.85rem;
            background: var(--bg);
            border-radius: 20px;
            font-size: 0.78rem;
            font-weight: 500;
            color: var(--secondary);
        }

        .back-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 14px;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.25s;
            text-decoration: none;
            box-shadow: 0 2px 8px rgba(20, 39, 78, 0.15);
        }

        .back-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(20, 39, 78, 0.25);
            background: var(--secondary);
        }

        /* === FOOTER === */
        .footer {
            background: var(--white);
            border-top: 1px solid rgba(155, 164, 180, 0.2);
            padding: 1.5rem 0;
        }

        .footer-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.825rem;
            color: var(--muted);
        }

        .footer-link {
            color: var(--secondary);
            text-decoration: none;
            font-weight: 500;
        }

        .footer-link:hover {
            color: var(--primary);
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 0.75rem;
            }

            .coming-soon-card {
                padding: 2.5rem 1.5rem;
                border-radius: 20px;
            }

            .coming-soon-title {
                font-size: 1.6rem;
            }

            .footer-content {
                flex-direction: column;
                gap: 0.5rem;
                text-align: center;
            }
        }
    </style>
</head>

<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <div class="logo">Quizzie</div>
            <nav>
                <ul class="nav-links">
                    <li><a href="/homeStudent">Dashboard</a></li>
                    <li><a href="/myClasses">My Classes</a></li>
                    <li><a href="/profileStudent">My Profile</a></li>
                </ul>
            </nav>
            <div class="header-right">
                <button class="header-icon" title="Notifications"><i data-lucide="bell"
                        style="width:18px;height:18px"></i></button>
                <a href="/profileStudent" class="header-profile" title="My Profile">
                    <span class="profile-initials">SK</span>
                </a>
            </div>
        </div>
    </header>

    <!-- Coming Soon Content -->
    <div class="coming-soon-container">
        <div class="coming-soon-card">
            <div class="icon-container">
                <i data-lucide="rocket" style="width:44px;height:44px"></i>
            </div>
            <h1 class="coming-soon-title">Coming Soon!</h1>
            <p class="coming-soon-subtitle">We're building something amazing</p>
            <p class="coming-soon-desc">
                This feature is currently under development. Stay tuned for exciting new updates that will enhance your
                learning experience!
            </p>
            <div class="feature-chips">
                <span class="feature-chip"><i data-lucide="sparkles" style="width:14px;height:14px"></i> AI
                    Quizzes</span>
                <span class="feature-chip"><i data-lucide="message-circle" style="width:14px;height:14px"></i> Class
                    Chat</span>
                <span class="feature-chip"><i data-lucide="trophy" style="width:14px;height:14px"></i>
                    Achievements</span>
                <span class="feature-chip"><i data-lucide="calendar" style="width:14px;height:14px"></i> Schedule</span>
                <span class="feature-chip"><i data-lucide="download" style="width:14px;height:14px"></i> Study
                    Materials</span>
            </div>
            <a href="/homeStudent" class="back-btn">
                <i data-lucide="arrow-left" style="width:18px;height:18px"></i> Back to Dashboard
            </a>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div>&copy; 2025 All rights reserved by www.Quizzie.in</div>
            <div><a href="/about-developers" class="footer-link">Meet Our Developers</a></div>
        </div>
    </footer>

    <script>
        const studentName = '{{{userName}}}';
        document.addEventListener('DOMContentLoaded', function () {
            lucide.createIcons();
            setProfileInitials();
        });

        function setProfileInitials() {
            const name = studentName || 'Student';
            const initials = name.split(' ').map(n => n[0]).join('').substring(0, 2).toUpperCase();
            document.querySelectorAll('.profile-initials').forEach(el => el.textContent = initials);
        }
    </script>
</body>

</html>